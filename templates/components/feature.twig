{% set feature_mod = "feature-#{index}" %}
<div class="square">
  <div class="square-content">
    <div class="feature {{ feature_mod }} h-100 d-flex flex-column p-3 p-md-5">
      <span class="small">{{ label }}</span>
      <p class="h3 mt-auto mb-0">{{ title }}</p>
    </div>
  </div>
</div>