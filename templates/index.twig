{% extends "base.twig" %}

{% block content %}
  {% include 'components/navbar.twig' %}
  {% include 'components/cover.twig' %}
  {% include 'components/services.twig' with { 
    title: services.title,
    label: services.label,
    text: services.text,
    questions: services.questions 
  } %}
  {% include 'components/portfolio.twig' with { 
    title: portfolio.title,
    label: portfolio.label,
    text: portfolio.text,
    works: works, 
  } %}
  {% include 'components/process.twig' with { 
    title: process.title,
    features: process.features, 
  } %}
  {% include 'components/about.twig' with { 
    title: about.title,
    label: about.label,
    text: about.text,
    quote: about.quote, 
  } %}
{% endblock %}